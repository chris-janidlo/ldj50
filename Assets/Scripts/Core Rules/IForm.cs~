using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace LDJ50.GameState
{
    public interface IForm
    {
        IEnumerable<Vector2> LegalPositionsNextTurn (Vector2 currentPosition, Board board);
        HashSet<IForm> FormTransformations ();
    }
}
